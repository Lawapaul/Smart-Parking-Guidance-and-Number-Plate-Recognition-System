# Sample Data

This directory is reserved for lightweight sample artifacts that are safe to keep in Git.

Large videos, model checkpoints, and other heavy assets should stay in ignored paths such as `data/large_files/`.
